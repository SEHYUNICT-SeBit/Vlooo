# Vlooo 프로젝트 상태

> **마지막 업데이트**: 2026-02-11  
> **현재 단계**: 로컬 개발 환경 완료 → 프로덕션 배포 준비 중

---

## 📊 현재 상태

### ✅ 완료된 작업

#### 백엔드 (FastAPI)
- [x] PPT 파싱 (`/api/parse-ppt`)
- [x] AI 스크립트 생성 (`/api/generate-script`)
- [x] TTS 음성 합성 (`/api/generate-tts`)
- [x] 영상 렌더링 (`/api/render-video`)
- [x] 프로젝트 상태 추적 (`/api/project-status/{projectId}`)
- [x] 체크포인트 시스템 (재개 로직)
- [x] Ollama 통합 (Llama 3.1)
- [x] FFmpeg 통합
- [x] Google TTS 통합

#### 프론트엔드 (Next.js)
- [x] PPT 업로드 UI
- [x] 변환 진행 상태 표시
- [x] 실시간 폴링 (3초 간격)
- [x] 단계별 진행 표시 (파싱 → 스크립트 → TTS → 렌더링)
- [x] 에러 처리 및 재시도
- [x] 결과 다운로드
- [x] 데이터 하이드레이션 (재개 로직)

#### 모니터링 GUI
- [x] 백엔드 제어 (시작/중지/재시작)
- [x] 실시간 로그 뷰어
- [x] 프로젝트 상태 모니터링
- [x] 로컬/원격 백엔드 전환

#### 인프라 & 설정
- [x] 환경 변수 템플릿 (`.env.local.example`, `.env.production.example`)
- [x] 로컬 개발 가이드 (`SETUP.md`)
- [x] 배포 가이드 (`DEPLOYMENT.md`)
- [x] Git 보안 (`.gitignore` 업데이트)

---

## ⚙️ 현재 설정

### 로컬 환경
```
Backend:  http://localhost:8001 (FastAPI)
Frontend: http://localhost:3000 (Next.js)
Ollama:   http://localhost:11434 (Llama 3.1)
FFmpeg:   C:\vibe\Vlooo\ffmeg\bin\ffmpeg.exe
```

### AI 모델
- **LLM**: Ollama Llama 3.1 (로컬)
- **TTS**: Google Cloud Text-to-Speech
- **스크립트 톤**: IT 전문가 페르소나

### 파일 저장
- **로컬**: `backend/media/`
- **체크포인트**: `backend/media/checkpoints/`

---

## 🔄 변환 파이프라인 상태

| 단계 | 상태 | 평균 소요 시간 (8슬라이드 기준) |
|------|------|--------------------------------|
| 1. PPT 파싱 | ✅ 정상 | ~2초 |
| 2. AI 스크립트 생성 | ✅ 정상 | ~10초 (Ollama) |
| 3. TTS 음성 합성 | ✅ 정상 | ~50초 |
| 4. 영상 렌더링 | ✅ 정상 | ~30초 (1080p) |
| **총 소요 시간** | | **~90초** |

---

## ⚠️ 알려진 이슈

### 해결됨
- ~~OpenAI API 할당량 문제~~ → Ollama로 전환
- ~~FFmpeg 경로 인식 안됨~~ → `.env.local`에 정확한 경로 설정
- ~~포트 8000/8001 혼동~~ → `FASTAPI_PORT=8001` 명시
- ~~체크포인트 로드 안됨~~ → `get_project_status()` 수정
- ~~Health 체크 로그 과다~~ → 모니터 GUI 폴링 비활성화

### 진행 중
- 없음

### 계획 중
- [ ] Cloudflare R2 통합 (파일 스토리지)
- [ ] 프로덕션 배포 (VPS + Cloudflare Pages)
- [ ] 사용자 인증 강화
- [ ] 다국어 지원 (영어)

---

## 🎯 다음 단계

### 즉시 할 일
1. **로컬 환경 테스트 완료**
   - [ ] 새로운 PPT 업로드
   - [ ] 전체 파이프라인 실행
   - [ ] 최종 영상 다운로드 확인

2. **백엔드 재시작 후 Ollama 테스트**
   ```powershell
   # 백엔드 재시작
   # 모니터 GUI → Restart Backend
   ```

3. **전체 플로우 검증**
   - 파싱 → 스크립트 → TTS → 렌더링 → 다운로드

### 단기 목표 (1-2주)
- [ ] Cloudflare R2 스토리지 설정
- [ ] VPS 서버 준비 (GPU 포함)
- [ ] 프로덕션 환경 변수 설정
- [ ] 백엔드 Systemd 서비스 설정
- [ ] Nginx 리버스 프록시 설정
- [ ] SSL 인증서 발급

### 중기 목표 (1개월)
- [ ] Cloudflare Pages 프론트엔드 배포
- [ ] VPS 백엔드 배포
- [ ] Uptime 모니터링 설정
- [ ] 로그 수집 시스템
- [ ] 백업 자동화

---

## 📁 프로젝트 구조

```
C:\vibe\Vlooo\
├── backend/              # FastAPI 백엔드
│   ├── main.py          # 메인 애플리케이션
│   ├── app/
│   │   ├── routes/      # API 라우터
│   │   └── services/    # 비즈니스 로직
│   └── media/           # 업로드 파일 & 체크포인트
├── src/                 # Next.js 프론트엔드
│   ├── app/             # Next.js 13 App Router
│   ├── components/      # React 컴포넌트
│   └── services/        # API 클라이언트
├── backgui/             # 모니터링 GUI
│   └── monitor.py
├── .env.local           # 로컬 환경 변수 (Git 무시)
├── .env.local.example   # 환경 변수 템플릿
├── .env.production.example  # 프로덕션 템플릿
├── SETUP.md             # 로컬 개발 가이드 ⭐
├── DEPLOYMENT.md        # 배포 가이드 ⭐
└── STATUS.md            # 이 파일
```

---

## 🔗 유용한 링크

- [로컬 개발 가이드](./SETUP.md)
- [배포 가이드](./DEPLOYMENT.md)
- [환경 변수 템플릿](./.env.local.example)
- [Ollama 다운로드](https://ollama.ai/)
- [FFmpeg 다운로드](https://www.gyan.dev/ffmpeg/builds/)

---

## 📝 노트

### 중요한 설정 값
- `LOCAL_LLM_PROVIDER=ollama` - Ollama 사용 필수
- `FASTAPI_PORT=8001` - 포트 8001 고정
- `FFMPEG_PATH` - 실제 파일 경로 확인

### 문제 발생 시 확인 순서
1. Ollama 실행 여부 (`http://localhost:11434`)
2. FFmpeg 파일 존재 여부
3. 백엔드 포트 8001 확인
4. `.env.local` 파일 내용 확인
5. 백엔드 재시작

---

**마지막 테스트**: 2026-02-11 18:15 (Scripting 단계까지 확인)  
**다음 테스트**: Ollama 전환 후 전체 플로우 검증
